[project]
name = "scraper"
version = "0.1.0"
description = "Mindris AI Scraper service"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.58.0",
    "playwright-stealth>=2.0.3",
]

[build-system]
requires = ["uv_build>=0.11.3,<0.12.0"]
build-backend = "uv_build"
